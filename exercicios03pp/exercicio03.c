 #include <stdio.h>

int main() {
    int contador = 10;  // Variável de controle para a contagem regressiva

    while (contador >= 0) {
        printf("%d\n", contador);  // Mostrar o número na tela
        contador--; 

    printf("FIM!\n");  

    return 0;
}
